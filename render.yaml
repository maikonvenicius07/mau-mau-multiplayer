services:
  - type: web
    name: mau-mau-multiplayer
    runtime: node
    plan: free
    buildCommand: npm install --no-audit --no-fund && npm run verify
    startCommand: npm start
    healthCheckPath: /health
